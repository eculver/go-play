package main

import "fmt"

const (
	introPath      = "/Users/eculver/sync/src/tdt/episode_notes/intro.html"
	guestTplName   = "guest.tpl.txt"
	noguestTplName = "noguest.tpl.txt"
)

func main() {
	// try to download notes?
	// try to download media?

	// upload media to CDN

	// generate episode HTML

	// upload episode HTML to web server as draft
	// uploadHTML(html, destination, status, slug)

	// email Daniele
	fmt.Println("a")
}
